//steve Tan z5237560
//Date 03/11/2019

#include <stdio.h>
#include <string.h>
#define MAX 256
int main(void) {
    char array[MAX][MAX]={{'\0'}};
    char input[MAX];  
    
    int num = 0;
    
    
    while (fgets(input,MAX,stdin) != NULL) {
        int row = 0;
        int counter = 1;
        while (row <= num) {
            if (strcmp(input,array[num]) == 0) {
                counter = counter * 0;
            } else {
                
            }
            row ++;
        }
        if (counter != 0) {
            strcpy(array[num],input);
            fputs(input,stdout);
            num ++;            
        }
    }
    
    //char array1 = &c1;
    
    return 0;
}
